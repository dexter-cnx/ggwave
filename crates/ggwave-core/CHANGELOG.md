# Changelog

## 1.2.0

- Renamed the reusable Rust layer from `ggwave-mobile` to `ggwave-core`.
- Removed mobile/application naming from the public API.
- Added `ultrasonic` and `dedup` feature flags.
- Added configurable sample-rate construction for platform adapters.
