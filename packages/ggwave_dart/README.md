# ggwave_dart

Pure Dart definitions shared by ggwave clients. This package deliberately contains **no Flutter or FFI dependency**.

It provides protocol IDs, mobile tuning values, sequence deduplication, and the `GgWaveTransport` interface. Use `ggwave_flutter` for microphone/speaker/native Rust support.
