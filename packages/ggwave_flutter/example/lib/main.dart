import 'package:flutter/material.dart';
import 'package:ggwave_flutter/ggwave_flutter.dart';
void main() => runApp(const MaterialApp(home: Scaffold(body: Center(child: Text('ggwave_flutter example — see README for microphone permissions')))));
